services:
  wenyan-editor:
    image: ghcr.io/liumeixin/wenyan-editor:latest
    container_name: wenyan-editor
    restart: unless-stopped
    ports:
      - "18888:8080"
    volumes:
      - ./frontend:/app/frontend:ro
      - ./backend:/app/backend:ro
      - ./wenyan-data:/app/data
